{
  "schema_version": 1,
  "certificates": [
    {
      "claim_id": "UC-CHAIN-NEIGHBOR-DELIVERY-RECIPIENT-CONSUMED",
      "steps": [
        {
          "rule": "predicate-result",
          "example": "neighbor b proc left delivery consumed by empty recipient",
          "expected": true,
          "predicate": "neighbor_delivery_consumed_by_recipient"
        },
        {
          "rule": "predicate-result",
          "example": "self write buffer completion is not neighbor delivery",
          "expected": false,
          "predicate": "neighbor_delivery_consumed_by_recipient"
        },
        {
          "rule": "predicate-result",
          "example": "recipient pending upstream blocks delivered command",
          "expected": false,
          "predicate": "neighbor_delivery_consumed_by_recipient"
        },
        {
          "rule": "predicate-result",
          "example": "neighbor c write buffer delivery consumed by recipient append",
          "expected": true,
          "predicate": "neighbor_delivery_consumed_by_recipient"
        },
        {
          "rule": "predicate-result",
          "example": "neighbor c write buffer zero delivery consumed by recipient append",
          "expected": true,
          "predicate": "neighbor_delivery_consumed_by_recipient"
        },
        {
          "rule": "predicate-result",
          "example": "neighbor c standard signal delivery remains unconsumed",
          "expected": false,
          "predicate": "neighbor_delivery_consumed_by_recipient"
        }
      ]
    },
    {
      "claim_id": "UC-CHAIN-NEIGHBOR-DELIVERY-RECIPIENT-REJECTED",
      "steps": [
        {
          "rule": "predicate-result",
          "example": "neighbor c standard signal delivery rejected by recipient",
          "expected": true,
          "predicate": "neighbor_delivery_rejected_by_recipient"
        },
        {
          "rule": "predicate-result",
          "example": "neighbor b proc left delivery is consumed not rejected",
          "expected": false,
          "predicate": "neighbor_delivery_rejected_by_recipient"
        },
        {
          "rule": "predicate-result",
          "example": "self write buffer completion is not neighbor rejection",
          "expected": false,
          "predicate": "neighbor_delivery_rejected_by_recipient"
        }
      ]
    }
  ]
}
