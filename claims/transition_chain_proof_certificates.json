{
  "schema_version": 1,
  "certificates": [
    {
      "claim_id": "UC-CHAIN-NEIGHBOR-DELIVERY-RECIPIENT-CONSUMED",
      "steps": [
        {
          "rule": "manifest-example",
          "example": "neighbor b proc left delivery consumed by empty recipient",
          "expected": true
        },
        {
          "rule": "manifest-example",
          "example": "self write buffer completion is not neighbor delivery",
          "expected": false
        },
        {
          "rule": "manifest-example",
          "example": "recipient pending upstream blocks delivered command",
          "expected": false
        },
        {
          "rule": "manifest-example",
          "example": "neighbor c write buffer delivery remains unconsumed",
          "expected": false
        },
        {
          "rule": "manifest-example",
          "example": "neighbor c write buffer zero delivery remains unconsumed",
          "expected": false
        }
      ]
    },
    {
      "claim_id": "UC-CHAIN-NEIGHBOR-DELIVERY-RECIPIENT-REJECTED",
      "steps": [
        {
          "rule": "manifest-example",
          "example": "neighbor c write buffer delivery rejected by recipient",
          "expected": true
        },
        {
          "rule": "manifest-example",
          "example": "neighbor c write buffer zero delivery rejected by recipient",
          "expected": true
        },
        {
          "rule": "manifest-example",
          "example": "neighbor b proc left delivery is consumed not rejected",
          "expected": false
        },
        {
          "rule": "manifest-example",
          "example": "self write buffer completion is not neighbor rejection",
          "expected": false
        }
      ]
    }
  ]
}
